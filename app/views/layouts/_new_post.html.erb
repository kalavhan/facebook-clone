<%= form_for @post do |f| %>
  <%= f.text_area :content, size: "60x20", class: "textarea", placeholder: "What are you thinking, #{current_user.first_name.capitalize}?" %>
  <div class="actions">
    <%= f.submit "Create post", class: "new-post-button" %>
  </div>
<% end %>